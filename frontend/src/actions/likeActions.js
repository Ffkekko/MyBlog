/* import axios from 'axios'; */
import {

} from '../constants/likeConstants';

/* export const addLike = (postId, qty) => async (dispatch, getState) => {
  const { data } = await axios.get(`/api/posts/${postId}`);
  const {
    postLike: { postLikes },
  } = getState();
    dispatch({
      type: POSTLIKE_ADD_LIKE,
      payload: {
        post: data._id,
        numLikes: data.numLikes,
      },
    });
}; */

/* export const removePostLike = (postId) => (dispatch, getState) => {
  dispatch({ type: POSTLIKE_REMOVE_LIKE, payload: posttId });

  const {
    postLike: { postLikes },
  } = getState();
}; */
